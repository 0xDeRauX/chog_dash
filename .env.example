TWITTER_API_KEY=
X_BEARER_TOKEN=
THIRDWEB_SECRET_KEY=
# Optional: dedicated Solana RPC URL for the SPL holder counts. Must support
# large getProgramAccounts (BONK ~485MB) — the public mainnet-beta endpoint does;
# Helius's free tier does NOT, so leave this unset to use the public default.
SOL_RPC=
# Free API keys for native-coin holder counts:
TAOSTATS_API_KEY=          # TAO (Bittensor) account count
blockvision_api_key_sui=   # SUI native holders
blockvision_api_key_monad= # MON (reserved — no clean count endpoint yet)
