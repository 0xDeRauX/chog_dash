TWITTER_API_KEY=
X_BEARER_TOKEN=
THIRDWEB_SECRET_KEY=
# Optional: dedicated Solana RPC URL for keyless holder counts (higher rate
# limits than the public endpoint, e.g. a free Helius URL). Falls back to
# api.mainnet-beta.solana.com if unset.
SOL_RPC=
